{% trans_default_domain 'FOSUserBundle' %}

<div class="container">
    <h2 class="ui header">Réinitialisation de mot de passe</h2>
    <form action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="fos_user_resetting_request">
      <div class="ui form">
        <div>
            <label for="username">{{ 'resetting.request.username'|trans }}</label>
            <input type="text" id="username" name="username" required="required" />
        </div>
        <button  type="submit" href="{{ path('homepage') }}" class="btn waves-effect waves-light">
          Validez puis allez vérifiez votre mail
          <!-- <input value="{{ 'resetting.request.submit'|trans }}" /> -->
        </button>
        <br><br>
      </div>
    </form>
</div>
